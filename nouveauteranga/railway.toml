[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build && npm install serve"

[deploy]
startCommand = "npx serve dist -s -p ${PORT:-3000}"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
